// Aula 07 - Operadores Lógicos

fun main(args: Array<String>) {

    // Operador || - OU/OR: É verdadeiro quando pelo menos uma das premissas é verdadeira

    if (20 == 20 || 21 < 20) {
        println("Verdadeiro")
    } else {
        println("Falso")
    }
}